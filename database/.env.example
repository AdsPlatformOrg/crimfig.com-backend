NODE_ENV=development

# PostgreSQL Connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=crimfig_core
DB_USER=crimfig_user
DB_PASSWORD=
DB_SSL=false

# Optional: Full connection string (overrides individual fields if set)
# DATABASE_URL=postgresql://crimfig_user:password@localhost:5432/crimfig_core
